<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vissionsikkimnew">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".category3"></activity>
        <activity android:name=".category" />
        <activity android:name=".category1" />
        <activity android:name=".helpdesk1" />
        <activity android:name=".userprofile1" />
        <activity android:name=".adminhomepage" />
        <activity android:name=".adminlogin" />
        <activity android:name=".userhomepage" />
        <activity android:name=".otpscreen" />
        <activity android:name=".loginactivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>
     
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>